#include <iostream>
#include<string>
using namespace std;

int main() {
    
    string str="Hello";
    int l=str.length();
    // cout<<l;
    while(l--)
    {
        cout<<str[l];
    }
    return 0;
    
}
