#include <iostream>
#include<string>
using namespace std;

int main() {
    
    string str="racer";
    int l=str.length();
    
    string str2;
    while(l--)
    {
      str2+=str[l];
    }
    
    if(str==str2)cout<<"palindrome";
    else cout<<"not palindrome";
    return 0;
    
}
